services:
  - type: web
    name: navarasa-ai-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --timeout 300 --worker-class gthread --threads 2 --bind 0.0.0.0:10000
    plan: free
